# FZF clipboard manager plugin
### ZSH plugin to fuzzy search and set into main clipboard items managed by "clipboard indicator" GNOME extension (need to be installed)
### JQ (json processor) must be installed as well

